# 🎨 Mood Palette

> **Curated Colors for Your Next Masterpiece.**
> Vibe Coding 期末專題作品

<img width="1900" height="866" alt="Demo" src="https://github.com/user-attachments/assets/cd97cb39-8372-4f3e-9851-122e64031dfd" />


## 📖 專案簡介 (About)

**Mood Palette** 是一個結合設計美感與 AI 運算的智慧配色工具。

我們發現許多人在創作時常面臨「選色障礙」。雖然市面上已有強大的配色網站（如 Coolors），但它們多依賴隨機生成，難以直觀地根據創作者腦中的「氛圍（Vibe）」來尋找顏色。

Mood Palette 不做隨機亂數，而是透過 **AI 美感篩選**，讓使用者僅需選擇風格關鍵字（如 `Retro`, `Cyberpunk`, `Pastel`），即可獲得一組協調、具備故事性的色彩組合。

---

## 💡 問題與動機 (Problem & Motivation)

### 遇到的問題
1. **創作瓶頸：** 繪圖或設計時，構圖往往很快完成，但最後常卡在「配色」上。
2. **現有工具限制：** 現有的配色網站大多是「隨機生成」，且許多進階功能（如色票管理）需要付費解鎖。
3. **抽象轉化困難：** 我們腦中常有一種「感覺」（例如：想要復古感、想要賽博龐克風），但不知道具體的 **Hex 色碼** 是什麼。

### 解決方案
* **關鍵字導向：** 不需要調整複雜參數，只需要選擇「風格關鍵字」，就能獲得經過美感篩選的色彩組合。
* **個人化收藏：** 提供自定義與儲存功能，建立屬於自己的靈感資料庫。

---

## ✨ 核心功能 (Features)

- **🏷️ 風格化檢索 (Vibe Filtering)**：點擊標籤（Tags）即可篩選出符合當下心境的配色（如暖色調、冷色調、復古風）。
- **📋 一鍵複製 (Click to Copy)**：點擊色票即可複製 Hex Code，並提供流暢的互動回饋。
- **➕ 自定義色票 (Create Palette)**：支援手動建立色票，並內建 Hex 格式自動修正與驗證功能。
- **📱 響應式設計 (Responsive)**：無論在電腦或手機上，都能享受沉浸式的瀑布流視覺體驗。

---

## 🤖 AI 協作說明 (AI Collaboration)

本專案運用 **Gen-AI** 協助開發，實現「AI 賦能」的創作流程：

| 協作項目 | 使用工具 | 說明 |
| :--- | :--- | :--- |
| **資料生成** | **Gemini 1.5 Pro** | 生成 JSON 格式的色票資料庫，包含配色 ID、創意命名（如 "Cyber Mist"）與風格標籤。 |
| **前端開發** | **Gemini / ChatGPT** | 生成 React 元件架構（如 Modal 視窗），並優化 Tailwind CSS 的 UI 細節。 |
| **除錯優化** | **ChatGPT** | 協助解決正規表達式 (Regex) 的 Hex 驗證邏輯與 RWD 排版問題。 |

---

## 🛠️ 技術堆疊 (Tech Stack)

* **Frontend Framework:** React (Hooks)
* **Language:** TypeScript
* **Styling:** Tailwind CSS
* **Icons:** Lucide React
* **Build Tool:** Vite

---

## 📂 程式碼精選 (Code Highlight)

為了優化使用者體驗，我在「新增色票」的功能中加入了嚴格的格式驗證與自動修正邏輯。以下是 `AddPaletteModal` 的核心實作：

```tsx
// 自動修正與驗證邏輯
const handleColorChange = (index: number, value: string) => {
  const newColors = [...colors];
  
  // UX 優化：若使用者輸入 6 位數且忘記加 #，程式自動補上
  if (value.length === 6 && !value.startsWith('#')) {
    newColors[index] = '#' + value;
  } else {
    newColors[index] = value;
  }
  setColors(newColors);
};

// 使用 Regex 即時檢查 Hex 格式是否正確
const isValidHex = (hex: string) => /^#[0-9A-Fa-f]{6}$/.test(hex);
